import {BookSection} from "./BookSection";

export class SectionGroup {
  sections: Array<BookSection>;
  opponent: string; // this will be an object later
  player: string; // this will be an object later
}
